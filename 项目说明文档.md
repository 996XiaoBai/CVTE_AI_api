# CVTE AI API (Dify Framework) 项目文档

## 📖 项目概述
本项目是一个基于 Dify API 构建的智能助手客户端，旨在为用户提供高效的 AI 交互体验。项目采用分层架构设计，支持 **命令行 (CLI)** 和 **Web UI** 两种交互方式，集成了智能对话、多格式文件解析、多媒体处理及语音转换等功能。

---

## 🚀 核心特性

*   **双重交互模式**：
    *   **CLI 模式**：轻量级，适合开发调试、自动化脚本及批量处理。
    *   **Web UI 模式**：基于 Streamlit，界面友好，适合团队协作、演示及复杂文件分析。
*   **强大的文件处理**：
    *   支持解析 PDF, PPTX, Excel, XMind, Markdown, 代码文件等多种格式。
    *   支持图片、视频、音频等多媒体文件的上传与处理。
*   **健壮的架构设计**：
    *   内置 HTTP 连接池与指数退避重试机制（自动处理 500/502/503/504 错误）。
    *   完善的日志记录系统（按日期自动切割）。
*   **流式响应**：支持 Real-time Streaming 输出，降低感知延迟。
*   **多媒体支持**：集成语音转文字 (STT) 和文字转语音 (TTS) 能力。

---

## 🛠 技术栈

*   **开发语言**: Python 3.7+
*   **核心框架**: Streamlit (Web UI)
*   **HTTP 客户端**: Requests (Session 管理)
*   **文档解析**: PyPDF2, python-pptx, pandas, xmindparser
*   **API 服务**: Dify 平台

---

## 📂 项目结构

```text
CVTE_AI_api/
├── main.py                     # [入口] 命令行启动程序
├── web_ui.py                   # [入口] Web UI 启动程序（streamlit run web_ui.py）
├── README.md                   # 项目说明文档
├── conf/                       # [配置层]
│   └── config.py               # 全局配置文件 (API Key, URL 等)
├── BasePage/                   # [基础层]
│   └── base_api.py             # HTTP 客户端封装 (重试、鉴权、连接池)
├── PageObject/                 # [业务层]
│   └── dify_service.py         # Dify 业务逻辑 (对话、文件、音频)
├── utils/                      # [工具层]
│   ├── file_parser.py          # 通用文件解析器
│   ├── logger.py               # 日志管理器
│   └── path_manager.py         # 路径管理工具
├── files/                      # [存储] 文件暂存区
└── logs/                       # [存储] 运行日志