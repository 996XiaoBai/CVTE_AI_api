# CVTE AI API 完整使用文档

## 目录
1. [项目简介](#项目简介)
2. [环境准备](#环境准备)
3. [安装与配置](#安装与配置)
4. [命令行模式使用](#命令行模式使用)
5. [Web界面模式使用](#web界面模式使用)
6. [功能详解](#功能详解)
7. [配置说明](#配置说明)
8. [常见问题解答](#常见问题解答)

## 项目简介

CVTE AI API 是一个基于 Dify 平台构建的智能助手应用程序，它集成了人工智能对话、文件处理和多模态交互功能。该项目提供了两种交互方式：

1. **命令行模式** - 适合开发者和技术人员使用
2. **Web界面模式** - 适合普通用户和非技术人员使用

项目支持多种文件格式的解析和上传，包括文档、图片、音视频等多种类型，可以满足日常工作中的多样化需求。

## 环境准备

### 系统要求
- Python 3.7 或更高版本
- 支持的操作系统：Windows、macOS、Linux

### 依赖库
项目所需的依赖库已在 requirements.txt 文件中列出：
```
# Core dependencies for CVTE AI API project
requests>=2.25.0
streamlit>=1.0.0
PyPDF2>=3.0.0
python-pptx>=0.6.0
pandas>=1.3.0
xmindparser>=1.0.0
```

## 安装与配置

### 1. 克隆或下载项目
```bash
git clone <项目地址>
cd CVTE_AI_api
```

### 2. 创建虚拟环境（推荐）
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate     # Windows
```

### 3. 安装依赖
```bash
pip install -r requirements.txt
```

### 4. 配置API密钥
编辑 `conf/config.py` 文件：
```python
class Config:
    # Dify API基础URL
    API_BASE_URL = "https://dify.cvte.com/v1"
    
    # 替换为您的API Key
    API_KEY = "your-api-key-here"
    
    # 用户标识（建议使用个人域账号）
    USER_ID = "your-user-id"
```

## 命令行模式使用

### 启动程序
```bash
python main.py
```

### 基本操作
程序启动后，您将看到操作指南：
```
============================================================
💡 操作指南：
   1. 正常聊天：直接输入文字，按回车发送。
   2. 上传图片：输入 '/add'，然后把文件拖进来。
   3. 清空文件：输入 '/clear' 清空当前已挂载的文件。
   4. 退出程序：输入 'q' 或 'exit'。
============================================================
```

### 详细操作说明

#### 正常聊天
直接输入您想说的话，按回车发送给AI：
```
👉 你: 你好，你能帮我做什么？
```

#### 静态文件配置
如果希望每次启动时自动上传某些文件，可以在 [main.py](file:///Users/linkb/PycharmProjects/CVTE_AI_api/main.py) 中配置：
```python
static_file_names = [
    # "cat.png",      # 在此处取消注释以启用启动时自动上传
    # "report.pdf",
]
```
取消注释相应行即可在启动时自动上传文件。

#### 动态添加文件
在聊天过程中，可以通过以下步骤上传文件：
1. 输入 `/add`
2. 将文件拖入终端窗口，或粘贴文件的绝对路径
3. 文件将随下一条消息一起发送

示例：
```
👉 你: /add
📂 [上传模式] 请将文件拖入此窗口，或粘贴绝对路径 (直接回车取消):
   文件路径 > /Users/username/Documents/sample.pdf
   ✅ 已成功挂载: sample.pdf
   (文件将随下一条消息一起发送)

👉 你 (已挂载 1 个文件): 请分析这份文档的内容
```

#### 清空文件
如果想要清除已挂载但尚未发送的文件，可以输入 `/clear`：
```
👉 你: /clear
🗑️ 已清空暂存文件。
```

#### 退出程序
输入 `q`、`quit` 或 `exit` 退出程序：
```
👉 你: exit
👋 再见！
```

## Web界面模式使用

### 启动程序
```bash
streamlit run web_ui.py
```

启动成功后，将在终端中看到类似以下的输出：
```
You can now view your Streamlit app in your browser.

Local URL: http://localhost:8501
Network URL: http://192.168.1.100:8501
```

### 界面介绍

#### 主界面
- 顶部显示应用标题"💬 Dify 全能助手"
- 下方显示对话历史记录
- 底部输入框用于输入消息

#### 侧边栏
- 会话控制：新开对话按钮
- 高级设置：可调整文档字数限制滑块

#### 文件挂载区
点击"📎 挂载文件"按钮打开文件选择器，支持的文件格式包括：
- 图像：JPG, PNG, WebP, GIF等
- 音视频：MP3, MP4等
- 文档：PDF, PPTX, XLSX, XMIND, TXT, MD等代码文件

### 使用流程

#### 新开对话
点击侧边栏的"🗑️ 新开对话"按钮可以开始新的对话。

#### 上传文件
1. 点击"📎 挂载文件"按钮
2. 点击"选择文件"或拖拽文件到选择区域
3. 选择一个或多个文件
4. 系统会显示"已就绪 X 个文件"

#### 发送消息
在底部输入框输入消息并按回车发送，系统会自动处理已挂载的文件。

#### 调整字数限制
在侧边栏的高级设置中，可以调整"单次文档字数限制"滑块，默认值为30000字符，最大支持500000字符。

## 功能详解

### 支持的文件类型

#### 多媒体文件
- 图像：.jpg, .jpeg, .png, .webp, .gif, .svg, .bmp, .tiff
- 视频：.mp4, .mov, .mpeg, .mpga, .webm, .avi
- 音频：.mp3, .m4a, .wav, .amr, .wma, .aac, .flac

#### 文档文件
- PDF文档：.pdf
- Office文档：.pptx, .xlsx, .xls, .docx, .doc
- 文本文件：.txt, .md, .markdown
- 网页：.html, .htm
- 数据文件：.csv, .xml
- 代码文件：.py, .java, .js, .html, .css, .json, .sql, .sh, .c, .cpp
- 思维导图：.xmind

### 文件处理流程

#### 文本文件处理
1. 系统自动解析文本文件内容
2. 根据配置的字数限制进行截断（防止Token溢出）
3. 将文件内容附加到用户消息后发送给AI

#### 多媒体文件处理
1. 多媒体文件直接上传到Dify平台
2. 上传成功后获得文件ID
3. 将文件ID和类型信息随消息一起发送

### 会话管理
- 系统自动维护会话ID
- 支持新建会话（清空历史记录）
- 会话数据保存在Dify平台

### 日志记录
- 程序运行过程中会自动生成日志文件
- 日志文件保存在项目根目录的`logs`文件夹中
- 日志按天分割，保留最近7天的日志

## 配置说明

### config.py 配置项详解

```python
class Config:
    # Dify API基础URL
    API_BASE_URL = "https://dify.cvte.com/v1"

    # API密钥（需替换为自己的密钥）
    API_KEY = "your-api-key-here"

    # 用户标识（建议使用个人域账号）
    USER_ID = "your-user-id"

    # 请求超时时间（秒）
    TIMEOUT = 30

    # 最大重试次数
    MAX_RETRIES = 3

    # 日志级别
    LOG_LEVEL = "INFO"

    # 日志保留天数
    LOG_Retention_Days = 7

    # 单次发送给AI的最大字符数
    MAX_CONTEXT_CHARS = 200000
```

### 配置项说明

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| API_BASE_URL | Dify API的基础URL | https://dify.cvte.com/v1 |
| API_KEY | API访问密钥，需要从Dify平台获取 | 需要配置 |
| USER_ID | 用户标识，建议使用个人域账号 | 需要配置 |
| TIMEOUT | 请求超时时间（秒） | 30 |
| MAX_RETRIES | 最大重试次数 | 3 |
| LOG_LEVEL | 日志级别（DEBUG/INFO/WARNING/ERROR） | INFO |
| LOG_Retention_Days | 日志保留天数 | 7 |
| MAX_CONTEXT_CHARS | 单次发送给AI的最大字符数 | 200000 |

## 常见问题解答

### 1. 连接失败
**问题现象**：启动程序时提示连接失败
**可能原因**：
- API_KEY配置错误
- 网络连接问题
- Dify服务不可用

**解决方案**：
1. 检查`conf/config.py`中的API_KEY是否正确
2. 检查网络连接是否正常
3. 确认Dify服务是否可用

### 2. 文件上传失败
**问题现象**：上传文件时提示失败
**可能原因**：
- 文件路径错误
- 文件格式不支持
- 文件过大

**解决方案**：
1. 检查文件路径是否正确
2. 确认文件格式是否在支持范围内
3. 检查文件大小是否超过限制

### 3. 文档内容被截断
**问题现象**：长文档的部分内容未被处理
**可能原因**：
- 超过默认的字符限制

**解决方案**：
1. 在Web界面中调整"单次文档字数限制"滑块
2. 或修改`conf/config.py`中的`MAX_CONTEXT_CHARS`值

### 4. Web界面无法启动
**问题现象**：执行命令后无法打开Web界面
**可能原因**：
- 未安装Streamlit
- 端口被占用

**解决方案**：
1. 确保已安装Streamlit：`pip install streamlit`
2. 检查端口是否被占用，或尝试指定其他端口：`streamlit run web_ui.py --server.port 8502`

### 5. 程序响应缓慢
**问题现象**：发送消息后AI回复很慢
**可能原因**：
- 网络延迟高
- 处理的文件较大
- Dify服务负载较高

**解决方案**：
1. 检查网络连接状况
2. 减少上传文件的大小或数量
3. 稍后再试

### 6. 如何获取API Key？
1. 访问Dify平台
2. 登录您的账户
3. 进入应用管理页面
4. 选择对应的应用
5. 在应用设置中找到API Keys选项
6. 创建新的API Key或使用现有的Key

### 7. 支持哪些文件格式？
项目支持多种文件格式，具体包括：
- 文档类：PDF、PPTX、XLSX、DOCX等
- 图像类：JPG、PNG、GIF、WEBP等
- 音视频类：MP3、MP4等
- 代码类：PY、JAVA、JS、HTML等
- 其他：TXT、MD、CSV、XMIND等

### 8. 单次可以上传多少个文件？
为了保证服务稳定性和响应速度，系统限制单次最多上传6个文件。

### 9. 日志文件在哪里查看？
日志文件保存在项目根目录的`logs`文件夹中，按照日期进行分割存储。

### 10. 如何反馈问题或建议？
可以通过以下方式反馈问题或建议：
1. 查看日志文件定位问题
2. 联系项目维护人员
3. 在项目仓库中提交Issue

## 附录

### 项目目录结构
```
CVTE_AI_api/
├── BasePage/           # 基础API客户端封装
├── PageObject/         # 业务逻辑层，封装Dify API调用
├── conf/               # 配置文件目录
├── utils/              # 工具类目录
├── files/              # 用户上传文件存储目录
├── logs/               # 日志文件存储目录
├── main.py             # 命令行主程序入口
├── web_ui.py           # Web UI程序入口
├── requirements.txt    # 项目依赖列表
└── README.md           # 项目说明文件
```

### 技术支持
如有其他问题，请联系项目维护人员或查阅相关技术文档。